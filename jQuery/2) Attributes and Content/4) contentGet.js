$(function() {
    var val = $("p").html(); // html() sets or returns the content of selected elements (including HTML markup).
    var val2 = $("p").text(); // text() sets or returns the text content of selected elements.
    alert(val);
    alert(val2)
});
