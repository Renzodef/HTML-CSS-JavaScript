$(function() {
    // The addClass( ) method adds one or more classes to the selected elements.
    $("div").addClass("header");
    // The removeClass() method removes one or more class names from the selected elements.
    $("#remove").removeClass("header");

});